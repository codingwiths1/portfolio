import 'package:flutter/material.dart';

class TMobileAbout extends StatelessWidget {
  const TMobileAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
